ADMIN_PASSWORD=dirgham2025

# Vercel Blob — required for image uploads on Vercel (auto-set when Blob store is linked)
# BLOB_READ_WRITE_TOKEN=

# Enable 3D hero scene (local dev only — keep unset on Vercel)
# NEXT_PUBLIC_ENABLE_3D=true

# ─── Google Gemini AI (free tier — https://aistudio.google.com/apikey) ───
GEMINI_API_KEY=
# AI_PROVIDER=gemini
# AI_MODEL_GEMINI=gemini-2.0-flash-lite
# AI_RATE_LIMIT_PER_MINUTE=8

# Optional fallback (only if AI_PROVIDER=auto or openrouter)
# OPENROUTER_API_KEY=
# AI_PROVIDER=auto
